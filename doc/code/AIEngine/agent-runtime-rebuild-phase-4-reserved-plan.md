# 智能体平台重构阶段 4：预留阶段计划

## 阶段目标

本阶段暂时留空，不定义实施内容。

该阶段只作为后续确认入口，不能被视为已经授权目录迁移、接口改名、数据库修改或能力扩展。

## 当前状态

```text
未确认实施范围
未确认涉及模块
未确认是否修改接口路径
未确认是否迁移 ai-engine -> ai-runtime
未确认是否迁移 ai-platform -> ai-admin 外部路径
未确认是否迁移 knowledge-bot/knowledge-base -> knowledge 外部路径
```

## 暂不授权内容

```text
不执行源码修改
不执行目录迁移
不执行数据库 migration
不修改前端接口路径
不新增 Rerank
不新增 Evaluation
不新增 Memory
不新增 Multi Agent
不新增自主循环 Agent
```

## 后续确认方式

进入本阶段前，需要先补充：

```text
阶段目标
迁移范围
涉及文件
是否修改接口路径
是否修改数据库
回滚方案
验收命令
删除规则
```

## 验收计划

当前无实施内容，因此无通过验收。

若后续补充实施范围，必须重新形成可执行计划和验收计划后再开始编码。
