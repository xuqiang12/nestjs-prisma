## 火山引擎向量化API

https://www.volcengine.com/docs/82379/1523520?redirect=1&lang=zh

## 硅基模型

https://cloud.siliconflow.cn/me/account/ak

# Navicat 连接数据库

本文档记录当前项目使用 Navicat 连接 Neon 远程数据库和本地 Docker/PostgreSQL 的方式。

## 连接 Neon 远程数据库

在 Navicat 中新建 `PostgreSQL` 连接，推荐使用 `.env` 里的 `NEON_DIRECT_URL` 对应信息，不使用带 `-pooler` 的连接池地址。

连接信息：

```text
连接名：nestjs-prisma-neon
主机：ep-square-river-aobv8gx7.c-2.ap-southeast-1.aws.neon.tech
端口：5432
初始数据库：neondb
用户名：neondb_owner
密码：npg_dBtlTNpXU62k
SSL：开启，模式选择 Require
网址：https://console.neon.tech/app/projects/proud-sky-90061400?database=neondb
```

说明：

- `NEON_DATABASE_URL` 是带 `-pooler` 的连接池地址，主要给 NestJS 应用运行使用。
- `NEON_DIRECT_URL` 是不带 `-pooler` 的直连地址，推荐给 Navicat、Prisma migrate、Prisma generate 使用。
- 如果连接成功但看不到表，先确认 Neon 数据库已经执行过迁移：`npx prisma migrate deploy`。
- 如果使用向量表，需要先在 Neon 的 SQL Editor 中执行：`CREATE EXTENSION IF NOT EXISTS vector;`。

## 连接本地 Docker/PostgreSQL

在 Navicat 中新建 `PostgreSQL` 连接：

```text
连接名：nestjs-prisma-local
主机：localhost
端口：5432
初始数据库：blog_db
用户名：postgres
密码：123456
SSL：关闭
```
